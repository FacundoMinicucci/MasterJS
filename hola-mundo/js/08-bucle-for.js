'use strict'

// Bucle FOR
var numero = 100;

/**for (
    let i = 0; // Contador.
    i < numero; // Condición de iteración.
    i++) // Incrementador.
    {
    const element = array[i];    
}*/

for (let i = 0; i <= numero; i++) {
    console.log("Vamos por el número: " + i);

    debugger; // Para debuggear en consola.
}